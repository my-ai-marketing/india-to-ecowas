(()=>{
  const sections=[...document.querySelectorAll('main > section[id]')];
  const sourceLinks=[...document.querySelectorAll('.side .nav a')];
  if(!sections.length||!sourceLinks.length) return;
  const items=sourceLinks.map((link,index)=>({href:link.getAttribute('href'),label:link.textContent.trim(),num:String(index+1).padStart(2,'0'),title:link.textContent.replace(/^\d+\s*\/\s*/,'').trim(),id:(link.getAttribute('href')||'').replace('#','')})).filter(item=>item.id);
  const chapter=document.createElement('button');
  chapter.className='study-chapter';chapter.type='button';chapter.setAttribute('aria-label','Open chapter navigation');chapter.setAttribute('aria-expanded','false');chapter.innerHTML='<span class="study-chapter-index">01</span><span class="study-chapter-copy"><small>Current chapter</small><span class="study-chapter-title">Executive summary</span></span><i class="study-chapter-chevron">⌄</i>';document.body.appendChild(chapter);
  const rail=document.createElement('nav');rail.className='study-rail';rail.setAttribute('aria-label','Section progress');rail.innerHTML=items.map(item=>`<button class="study-rail-dot" type="button" data-target="${item.id}" data-label="${item.label}" aria-label="Go to ${item.title}"></button>`).join('');document.body.appendChild(rail);
  const overlay=document.createElement('div');overlay.className='study-overlay';overlay.setAttribute('aria-hidden','true');overlay.innerHTML=`<div class="study-overlay-head"><div><div class="study-overlay-kicker">India → ECOWAS · Food Processing Feasibility</div><h2>Choose a chapter</h2><p>Move directly to location, sourcing, capital, operating cost, logistics, product economics or the investment decision.</p></div><button class="study-overlay-close" type="button" aria-label="Close chapter navigation">×</button></div><nav class="study-overlay-grid" aria-label="All feasibility chapters">${items.map(item=>`<a class="study-overlay-link" href="${item.href}" data-target="${item.id}"><span class="num">${item.num}</span><strong>${item.title}</strong><span>Open chapter ↗</span></a>`).join('')}</nav>`;document.body.appendChild(overlay);
  const mobileTop=document.createElement('div');mobileTop.className='study-mobile-top';mobileTop.innerHTML=`<div class="study-mobile-brand"><span class="study-mobile-mark">IE</span><span class="study-mobile-context"><b>India → ECOWAS</b><span class="study-mobile-section">01 / Executive summary</span></span></div><button class="study-mobile-menu" type="button" aria-label="Open section navigation" aria-expanded="false"><span>Menu</span><i class="study-mobile-icon" aria-hidden="true"></i></button><div class="study-mobile-progress" aria-hidden="true"><i></i></div>`;document.body.appendChild(mobileTop);
  const mobileDrawer=document.createElement('div');mobileDrawer.className='study-mobile-drawer';mobileDrawer.setAttribute('aria-hidden','true');mobileDrawer.innerHTML=`<div class="study-mobile-drawer-head"><div><div class="section-label" style="margin:0 0 6px">Navigate study</div><div class="study-mobile-drawer-title">Choose a section</div></div><button class="study-mobile-close" type="button" aria-label="Close navigation">×</button></div><nav class="study-mobile-nav" aria-label="Mobile section navigation">${items.map(item=>`<a href="${item.href}" data-target="${item.id}">${item.label}</a>`).join('')}</nav><div class="study-mobile-actions"><button class="btn" type="button" data-study-present>Present</button><button class="btn" type="button" data-study-pdf>Export PDF</button></div>`;document.body.appendChild(mobileDrawer);
  const mq=window.matchMedia('(min-width:1051px)'),closeOverlay=overlay.querySelector('.study-overlay-close'),railDots=[...rail.querySelectorAll('.study-rail-dot')],overlayLinks=[...overlay.querySelectorAll('.study-overlay-link')],mobileLinks=[...mobileDrawer.querySelectorAll('.study-mobile-nav a')],chapterIndex=chapter.querySelector('.study-chapter-index'),chapterTitle=chapter.querySelector('.study-chapter-title'),mobileSection=mobileTop.querySelector('.study-mobile-section'),mobileMenu=mobileTop.querySelector('.study-mobile-menu'),mobileClose=mobileDrawer.querySelector('.study-mobile-close'),mobileProgress=mobileTop.querySelector('.study-mobile-progress i');
  const setDesktopOpen=open=>{if(!mq.matches)open=false;document.body.classList.toggle('study-nav-open',open);chapter.setAttribute('aria-expanded',String(open));overlay.setAttribute('aria-hidden',String(!open));if(open)closeOverlay?.focus()};
  const setMobileOpen=open=>{if(mq.matches)open=false;document.body.classList.toggle('study-mobile-open',open);mobileMenu.setAttribute('aria-expanded',String(open));mobileDrawer.setAttribute('aria-hidden',String(!open));if(open)mobileClose?.focus()};
  const jumpTo=id=>{const target=document.getElementById(id);if(!target)return;setDesktopOpen(false);setMobileOpen(false);target.scrollIntoView({behavior:'smooth',block:'start'})};
  chapter.addEventListener('click',()=>setDesktopOpen(!document.body.classList.contains('study-nav-open')));closeOverlay?.addEventListener('click',()=>setDesktopOpen(false));overlay.addEventListener('click',e=>{if(e.target===overlay)setDesktopOpen(false)});railDots.forEach(dot=>dot.addEventListener('click',()=>jumpTo(dot.dataset.target)));overlayLinks.forEach(link=>link.addEventListener('click',e=>{e.preventDefault();jumpTo(link.dataset.target)}));mobileMenu.addEventListener('click',()=>setMobileOpen(true));mobileClose?.addEventListener('click',()=>setMobileOpen(false));mobileLinks.forEach(link=>link.addEventListener('click',e=>{e.preventDefault();jumpTo(link.dataset.target)}));
  mobileDrawer.querySelector('[data-study-present]')?.addEventListener('click',()=>{setMobileOpen(false);if(typeof window.togglePresent==='function')window.togglePresent()});mobileDrawer.querySelector('[data-study-pdf]')?.addEventListener('click',()=>{setMobileOpen(false);window.print()});
  const activate=id=>{const item=items.find(x=>x.id===id);if(!item)return;chapterIndex.textContent=item.num;chapterTitle.textContent=item.title;mobileSection.textContent=item.label;railDots.forEach(dot=>dot.classList.toggle('active',dot.dataset.target===id));overlayLinks.forEach(link=>link.classList.toggle('active',link.dataset.target===id));mobileLinks.forEach(link=>link.classList.toggle('active',link.dataset.target===id))};
  if('IntersectionObserver' in window){const observer=new IntersectionObserver(entries=>{const visible=entries.filter(entry=>entry.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio);if(visible[0])activate(visible[0].target.id)},{rootMargin:'-22% 0px -58% 0px',threshold:[0,.1,.25,.5]});sections.forEach(section=>observer.observe(section))}activate(items[0]?.id);
  let tick=false;const updateProgress=()=>{const max=document.documentElement.scrollHeight-window.innerHeight,pct=max>0?Math.min(100,Math.max(0,(window.scrollY/max)*100)):0;mobileProgress.style.width=`${Math.max(2,pct)}%`};window.addEventListener('scroll',()=>{if(tick)return;tick=true;requestAnimationFrame(()=>{updateProgress();tick=false})},{passive:true});updateProgress();
  document.addEventListener('keydown',e=>{if(e.key!=='Escape')return;if(document.body.classList.contains('study-nav-open')){e.preventDefault();setDesktopOpen(false);chapter.focus()}if(document.body.classList.contains('study-mobile-open')){e.preventDefault();setMobileOpen(false);mobileMenu.focus()}});mq.addEventListener?.('change',e=>{if(e.matches)setMobileOpen(false);else setDesktopOpen(false)});
})();